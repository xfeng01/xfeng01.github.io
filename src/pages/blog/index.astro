---
import BasicLayout from '../../layouts/BasicLayout.astro';
import { getCollection } from 'astro:content';
import BlogPostCard from '../../components/BlogPostCard.astro';

const posts = (await getCollection('blog', ({ data }) => !data.draft)).sort((a, b) => {
  return b.data.date.getTime() - a.data.date.getTime();
});
---

<BasicLayout title="Blog | Xinsong Feng" showProfile={false}>
  <section class="max-w-4xl">
    <div class="border-b border-gray-200 pb-5">
      <div class="max-w-2xl">
        <h2 class="mb-2 text-3xl font-semibold leading-tight text-gray-900">Blog</h2>
        <p class="mb-0 text-base leading-7 text-gray-600">
          Research notes, paper summaries, and implementation details.
        </p>
      </div>
    </div>

    {
      posts.length === 0 ? (
        <div class="border-b border-dashed border-gray-300 py-8 text-gray-600">
          No posts yet.
        </div>
      ) : (
        <div class="divide-y divide-gray-200">
          {posts.map((post) => <BlogPostCard post={post} />)}
        </div>
      )
    }
  </section>
</BasicLayout>
